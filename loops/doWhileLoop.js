// Start with:
let x = 10;
//Run a `do...while` whose condition is false from the beginning, but prove that the body still executes once.


do {
    console.log("Its working even the condition is wrong!")
} while (x>10);