// Perform these operations using assignment operators only:

// 1. Add `500`
// Subtract `200`
// Multiply the remaining value by `2`
// Divide it by `4`
// Store the remainder after dividing it by `300`

let balance = 1000;
balance += 500
balance -= 200
balance *= 2
balance /= 4
balance %= 300
